//React Context provides a way to share data between components without having to pass props down manually at every level
import {createContext} from "react";

export const authContext = createContext("");